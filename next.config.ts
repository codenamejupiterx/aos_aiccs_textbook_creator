// next.config.ts
/** @type {import('next').NextConfig} */
const nextConfig = {
  // no 'output: "standalone"' since we’re running with `next start`
  reactStrictMode: true,
};
export default nextConfig;
